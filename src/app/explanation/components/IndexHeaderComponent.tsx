import { Box } from "@mui/material";
import React from "react";

export function IndexHeaderComponent(): JSX.Element {
    return (
        <Box sx={{ margin: "0 auto", display: "grid" }}>
            {/* Content has been removed, and the component does not expect any props */}
            {/* Placeholder or static content can go here if needed */}
        </Box>
    );
}
